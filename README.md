# Mobile Frontend

Please see the [Main Project README](../README.md) for full documentation, setup instructions, and deployment details.

This directory contains the React Native + Expo mobile application source code. All commands to run the frontend (like `npx expo start` or `eas build`) should be executed from within this directory.
